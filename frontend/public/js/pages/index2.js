{/* <script>
  function aplicarResponsiveCards2() {
    const contenedor = document.querySelector(".cards__container");

    if (window.innerWidth <= 768) {
      contenedor.style.flexDirection = "column"; // apiladas en móvil
      contenedor.style.alignItems = "center";
    } else {
      contenedor.style.flexDirection = "row"; // columnas en escritorio
      contenedor.style.alignItems = "stretch";
    }
  }

  document.addEventListener("DOMContentLoaded", aplicarResponsiveCards2);
  window.addEventListener("resize", aplicarResponsiveCards2);
</script> */}
